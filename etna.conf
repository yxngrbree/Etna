PROJECTS=("db" "backend" "frontend")

PROJECT_DEPS["backend"]="db"
PROJECT_DEPS["frontend"]="backend"

PROJECT_STEPS["db"]="checkout|git checkout main && git pull origin main,install|npm install,migrate|./migrate.sh"
PROJECT_STEPS["backend"]="checkout|git checkout develop && git pull origin develop,install|npm install,lint|npm run lint,test|npm test,build|npm run build"
PROJECT_STEPS["frontend"]="checkout|git checkout main && git pull origin main,install|npm install,lint|npm run lint,test|npm test,build|npm run build,deploy|./deploy.sh"
